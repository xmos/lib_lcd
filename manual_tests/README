Hardware setup:
--------------
Connect the LCD slice to the circle slot of A16 board for single LCD test. 
Connect two slices, one to circle slot and the other to square slot for dual LCD test.


To run the testbenches: 
----------------------
Type the following commands in the command line. Power cycle the hardware after executing one command.

python runtests_manual.py --subtest test_single_lcd --resource-file resources.xml
python runtests_manual.py --subtest test_dual_lcd --resource-file resources.xml
